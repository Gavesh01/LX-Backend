services:
  - type: web
    name: liverxplain-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MODEL_PATH
        value: nb2_DeiT-S_dual_best.pth
